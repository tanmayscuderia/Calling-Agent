{
  "name": "calling-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Real Estate WhatsApp AI + Calling Agent Prototype",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:migrate": "psql \"$DATABASE_URL\" -f supabase/migrations/20260101_0000_core_schema.sql && psql \"$DATABASE_URL\" -f supabase/migrations/20260101_0001_real_estate_ai_prototype.sql && psql \"$DATABASE_URL\" -f supabase/migrations/20260101_0002_demo_seed.sql",
    "db:reset": "psql \"$DATABASE_URL\" -f supabase/migrations/20260101_0000_core_schema.sql"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}